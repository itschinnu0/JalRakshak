package com.jalrakshak.app.core.architecture

/**
 * Base convention for ViewModel state
 */
interface UiState

/**
 * Base convention for user actions or lifecycle events
 */
interface UiEvent

/**
 * Base convention for one-time effects (navigation, snackbar, haptics, short sounds)
 */
interface UiEffect
