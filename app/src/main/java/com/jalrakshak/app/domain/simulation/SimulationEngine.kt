package com.jalrakshak.app.domain.simulation

/**
 * Stub for deterministic data simulation engine.
 * Will generate predictable scenarios (Normal, Mining Water, Faults).
 */
interface SimulationEngine {
    // fun generateNextReading(): SensorReading
}
