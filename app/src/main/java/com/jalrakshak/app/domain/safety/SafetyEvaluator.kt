package com.jalrakshak.app.domain.safety

/**
 * Stub for the domain safety evaluator which will determine the SafetyState
 * from a given SensorReading.
 * Safety evaluation belongs purely in the domain layer.
 */
interface SafetyEvaluator {
    // fun evaluate(reading: SensorReading): SafetyState
}
